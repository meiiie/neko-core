[extend]
useDefault = true

[[allowlists]]
description = "Chrome Manifest public extension identity key"
targetRules = ["generic-api-key"]
condition = "AND"
regexTarget = "line"
paths = ['''^browser-extension/manifest\.json$''']
regexes = ['''^\s*"key"\s*:\s*"[A-Za-z0-9+/=]+"\s*,?\s*$''']
